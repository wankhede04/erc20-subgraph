type DepositedToBin @entity(immutable: true) {
  id: Bytes!
  sender: Bytes! # address
  to: Bytes! # address
  ids: [BigInt!]!
  amounts: [Bytes!]! # array of amounts in bytes
}

# Avoid using plural entity name, or else: Error: No values provided for `id`
